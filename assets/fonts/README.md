# Fonts

TaskTree auto-detects a system font (DejaVu / Liberation / Ubuntu) if none is bundled
here. To pin a specific UI font, drop a TrueType file named `Inter-Regular.ttf` (or
`UI.ttf`) into this directory — it takes priority over system fonts.

Fonts are intentionally not committed (licensing / binary size). Add your own.
